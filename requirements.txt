# Core (required)
PyMuPDF>=1.24

# Token counting — optional but recommended (falls back to char/4 estimate without it)
tiktoken>=0.5

# Optional: --images ocr  (offline, no API). Pick ONE:
#   pip-only, no extra binary:
# rapidocr-onnxruntime>=1.3
#   OR Tesseract (also needs the Tesseract binary installed):
# pytesseract>=0.3.10
# Pillow>=10.0

# Optional: --images describe  (Claude AI vision; needs ANTHROPIC_API_KEY env var)
# anthropic>=0.40
